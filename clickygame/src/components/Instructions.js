import React from "react";

//a component with the game instructions
function Instructions() {
    return (
        <div className="card">
            <div className="card-body">
                Click on the images to earn points, but don't click the same picture twice!
            </div>
        </div>
    )
}

export default Instructions;